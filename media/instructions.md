Upload a regular-sized image and name it `keyword.png`

Upload a thumbnail-sized 64x64 image and name it `keyword-64.png`

Add both of these references in the appropriate columns of the linked Google Sheet
